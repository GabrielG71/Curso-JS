const idade = 18;

const balada = idade >= 18 ? "Pode entrar" : "Não entra não";

console.log(`${balada}`);

const time = "Santos";
const time_padrao = time || "Real Madrid";

console.log(time_padrao);
